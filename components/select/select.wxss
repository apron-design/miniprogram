@import '../../styles/variables.wxss';

.apron-select {
  position: relative;
  display: inline-block;
  width: 100%;
  font-size: var(--font-size-base);
  outline: none;
  box-sizing: border-box;
}

.apron-select__trigger {
  display: flex;
  align-items: center;
  width: 100%;
  height: 80rpx;
  padding: 0 var(--spacing-4);
  background-color: var(--color-primary-50);
  border: 1px solid var(--color-primary-50);
  border-radius: var(--radius-lg);
  transition: all 0.3s ease;
  box-sizing: border-box;
  cursor: pointer;
}

.apron-select.active .apron-select__trigger {
  background-color: #ffffff;
  border-color: var(--color-primary-500);
}

.apron-select.disabled .apron-select__trigger {
  background-color: var(--color-neutral-100);
  border-color: var(--color-neutral-200);
  color: var(--color-neutral-400);
  cursor: not-allowed;
}

.apron-select__value {
  flex: 1;
  color: var(--color-primary-500);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.apron-select__value.placeholder {
  color: var(--color-primary-300);
}

.apron-select__suffix {
  display: flex;
  align-items: center;
  margin-left: var(--spacing-2);
  color: var(--color-primary-300);
}

.apron-select__arrow {
  font-size: 24rpx;
  transition: transform 0.3s ease;
}

.apron-select__arrow.open {
  transform: rotate(180deg);
}

.apron-select__loading-icon {
  font-size: 32rpx;
  animation: apron-select-spin 1s linear infinite;
}

@keyframes apron-select-spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.apron-select__dropdown {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  margin-top: var(--spacing-2);
  background-color: #ffffff;
  border: 1px solid var(--color-neutral-200);
  border-radius: var(--radius-lg);
  box-shadow: 0 20rpx 30rpx -10rpx rgba(0, 0, 0, 0.1);
  z-index: var(--z-index-dropdown);
  max-height: 400rpx;
  overflow-y: auto;
}

.apron-select__options {
  padding: var(--spacing-2) 0;
}

.apron-select__option {
  padding: var(--spacing-3) var(--spacing-4);
  color: var(--color-primary-500);
  cursor: pointer;
  transition: all 0.2s ease;
}

.apron-select__option:active {
  background-color: var(--color-neutral-100);
}

.apron-select__option.selected {
  color: var(--color-secondary-500);
  background-color: var(--color-neutral-100);
}

.apron-select__option.disabled {
  color: var(--color-neutral-300);
  cursor: not-allowed;
}

.apron-select.inflow {
  width: auto;
  min-width: 200rpx;
}

